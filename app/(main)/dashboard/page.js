import LiveDashboard from '@/components/dashboard/LiveDashboard';

export const metadata = { title: 'Live Dashboard | Concord TrackSync' };

export default function DashboardPage() {
  return <LiveDashboard />;
}

